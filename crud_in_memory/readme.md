
# User Management CRUD API (Express.js)

This is a simple CRUD (Create, Read, Update, Delete) REST API built using Node.js and Express.js.
It uses an in-memory array as a dummy database to manage users. 
Data is not persistent — restarting the server will reset all users.

## 🚀 Features
- Add a new user
- Get all users
- Get user by ID
- Update user details
- Delete a user