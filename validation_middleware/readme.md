
# 🚀 Express User Routes App

This is a basic Express.js application that demonstrates how to use routes in a clean way.
It uses a separate routes folder and handles user-related APIs under /users.

## 📌 Features

- Express server setup
- JSON request body handling
- Separate user routes (/users)
- In-memory CRUD ready 