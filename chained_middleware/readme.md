
# Chained Express Middleware 

This project demonstrates stacked middleware execution in Express.js using a simple admin route. It shows how multiple middleware functions work together in sequence before reaching the final route handler.

## 📌 Features
- Route parameter validation
-  Dummy authentication using headers
- Request logging
-  Middleware chaining (stacked middleware)