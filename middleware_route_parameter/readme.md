
# 📌 Express Route Parameter Validation
This project demonstrates how to use Express.js middleware to validate route parameters before processing a request.

## features 
- Setting up a basic Express server
- Using route parameters (:id) in URLs
- Writing a custom middleware function
- Validating whether a route parameter is a number
- Sending appropriate HTTP responses for valid and invalid requests